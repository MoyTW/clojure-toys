(ns formatter.extensions.thread-first-test
  (:require [clojure.test :refer :all]
            [formatter.parser :as par]
			[formatter.extensions.thread-first :as ext]))

(deftest thread-first-placeholder
  (testing "Placeholder test"
    (is false)))